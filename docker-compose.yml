services:
  db:
    image: postgres:16-alpine
    container_name: braviaprime-postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=braviaprime
    volumes:
      - db:/var/lib/postgresql/data
    networks:
      - braviaprime-network
  redis:
    image: redis
    container_name: braviaprime-redis
    ports:
      - 6379:6379
    volumes:
      - redis:/data
    networks:
      - braviaprime-network

  serverless-redis-http:
    image: hiett/serverless-redis-http:latest
    container_name: braviaprime-redis-http
    ports:
      - 8079:80
    environment:
      SRH_MODE: env
      SRH_TOKEN: braviaprime
      SRH_CONNECTION_STRING: redis://redis:6379
    networks:
      - braviaprime-network
    depends_on:
      - redis

volumes:
  db:
  redis:

networks:
  braviaprime-network:
    driver: bridge
